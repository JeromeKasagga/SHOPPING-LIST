const inputText = document.querySelector("input-box");
const addButton = document.querySelector("button");

addButton.addEventListener("click", function() {
    const shoppingItem = inputText.value
})